const defaultApi = "http://localhost:5000/api/v1";

export const expenseAPIS = `${defaultApi}/expenses`;

export const authAPIS = {
  login: `${defaultApi}/login`,
  signup: `${defaultApi}/signup`,
};
